name: Simulate Deploy to Staging

on:
  push:
    branches:
      - develop

jobs:
  simulate-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies (simulado)
        run: echo "Skipping npm install (simulation)"

      - name: Build app (simulado)
        run: echo "Skipping build step (simulation)"

      - name: Deploy to Staging (simulado)
        run: echo "Simulating deploy to staging server 🚀"
